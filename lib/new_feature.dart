import 'package:flutter/material.dart';

class NewFeature extends StatelessWidget {
  const NewFeature({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('This is a new feature!'),
    );
  }
}
